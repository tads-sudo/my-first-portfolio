import { createUseStyles } from "react-jss";

export const useStyles = createUseStyles({
  formInputLabel: {
    display: "flex",
    flexDirection: "row",
    alignItems: "flex-start",
    marginBottom: "8px",
    flex: "1",
  },
});
